# 🏀 Статистика Баскетболиста

Мобильное приложение для отслеживания статистики баскетболиста и просмотра информации об NBA, разработанное на Flutter. Приложение позволяет вести учет личных достижений (очки, ассисты), следить за состоянием здоровья и получать актуальные данные о командах и игроках NBA.

## ✨ Основные возможности

### 🏀 NBA Инфо
*   **Команды** — просмотр списка команд NBA с возможностью кэширования для оффлайн-доступа. Детальная информация о составе и последних играх.
*   **Игроки** — поиск игроков NBA по имени, просмотр физических данных и команды.
*   **Игры** — просмотр списка прошедших игр и их результатов.

### 📊 Личная статистика
*   **Очки** — ведение истории набранных очков за игры.
*   **Ассисты** — учет количества результативных передач с возможностью добавления и удаления.
*   **Травмы** — отслеживание статуса здоровья (Здоров/Травмирован) и история травм.

### 👤 Профиль
*   **Личный кабинет** — управление данными профиля (имя, игровой номер).
*   **Безопасность** — защита входа паролем.

### ⚙️ Дополнительные функции
*   **Настройки** — переключение темы оформления (светлая/темная).
*   **Оффлайн-режим** — локальное хранение личной статистики и кэширование данных NBA.

## 🏗️ Архитектура проекта

Приложение построено на принципах **Clean Architecture** с четким разделением на слои:

*   `core/` — Базовые модели, утилиты и хелперы.
*   `data/` — Слой данных (реализация API клиентов, локальная БД, реализации репозиториев, DTO).
*   `domain/` — Бизнес-логика (абстрактные интерфейсы репозиториев, модели предметной области).
*   `features/` — UI слой и реализация фич (экраны, cubits, виджеты).
*   `shared/` — Общие компоненты и внедрение зависимостей (Service Locator).

## 🛠️ Технологический стек

*   **Основной стек:** Flutter, Dart
*   **Управление состоянием:** `flutter_bloc` (Cubit)
*   **Навигация:** `go_router`
*   **Работа с данными:** `dio`, `retrofit`, `json_annotation`
*   **Локальное хранение:** `sqflite` (SQLite), `shared_preferences`, `flutter_secure_storage`
*   **Dependency Injection:** `get_it`
*   **UI компоненты:** `cached_network_image`, `cupertino_icons`

## 📦 Установка и запуск

### Требования
*   Flutter SDK
*   Dart SDK
*   Android Studio / VS Code

### Шаги установки

1.  **Клонирование репозитория**
    ```bash
    git clone <repository-url>
    cd flutter3
    ```

2.  **Установка зависимостей**
    ```bash
    flutter pub get
    ```

3.  **Генерация кода (для JSON и Retrofit)**
    ```bash
    dart run build_runner build --delete-conflicting-outputs
    ```

4.  **Запуск приложения**
    ```bash
    flutter run
    ```

## 🗄️ База данных

Приложение использует **SQLite** (через пакет `sqflite`) для хранения локальных данных.

**Таблицы:**
*   `assists` — история ассистов (ID, timestamp).
*   `points` — история очков (ID, значение, timestamp).
*   `injuries` — история травм (ID, статус, timestamp).
*   `teams` — кэш команд NBA (ID, название, город, конференция, дивизион).

**Secure Storage:**
Используется для безопасного хранения имени пользователя и пароля.

## 🌐 Интеграции с внешними API

### Balldontlie API
Используется для получения базовой информации об NBA:
*   Список команд
*   Поиск игроков
*   Список игр

### TheSportsDB API
Используется для получения расширенной информации:
*   Детали лиги и сезоны
*   Детальная информация о командах (логотипы, описание)
*   Составы команд и события

## 📱 Основные экраны

*   **Экран приветствия** (`/`) — Вход в приложение.
*   **Профиль** (`/stats`) — Главный экран со сводкой.
*   **NBA** (`/nba`) — Дашборд с вкладками Команды/Игроки/Игры.
*   **Очки** (`/points`) — Управление очками.
*   **Ассисты** (`/assists`) — Управление ассистами.
*   **Травмы** (`/injury`) — Статус здоровья.
*   **Настройки** (`/settings`) — Смена темы.

## 👤 Автор

**Кварталов Егор Алексеевич**

---
⭐ Проект создан в учебных целях.
